networks:
  open-brain:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  whisper_model_cache:
  financial_ingest_data:
  utility_ingest_data:
  admin_prewipe_backup:
  prometheus_data:
  grafana_data:
  loki_data:

services:
  postgres:
    image: pgvector/pgvector:pg16
    container_name: open-brain-postgres
    environment:
      POSTGRES_DB: openbrain
      POSTGRES_USER: openbrain
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-openbrain_dev}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./config/postgres/postgresql.conf:/etc/postgresql/postgresql.conf
    command: postgres -c config_file=/etc/postgresql/postgresql.conf
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U openbrain -d openbrain"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 8g
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  redis:
    image: redis:7-alpine
    container_name: open-brain-redis
    ports:
      - "6380:6379"
    volumes:
      - redis_data:/data
    command: redis-server --appendonly yes
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5
    mem_limit: 512m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  core-api:
    image: ghcr.io/davistroy/open-brain/core-api:latest
    build:
      context: .
      target: core-api
    container_name: open-brain-core-api
    env_file:
      - .env.secrets
    environment:
      NODE_ENV: production
      PORT: "3000"
      POSTGRES_URL: postgresql://openbrain:${POSTGRES_PASSWORD}@postgres:5432/openbrain
      REDIS_URL: redis://redis:6379
      OPENAI_BASE_URL: https://api.openai.com/v1
      OLLAMA_URL: http://ollama:11434/v1
      WIKI_REPO_URL: http://Gitea:3000/davistroy/open-brain-wiki.git
      WIKI_LOCAL_PATH: /tmp/open-brain-wiki
      GITEA_TOKEN: ${GITEA_TOKEN}
      NODE_OPTIONS: "--max-old-space-size=1200"
      # Himalaya TOML config lives on the mounted config volume. Credentials live
      # inside the TOML file (gitignored; sourced from Bitwarden BOND_EMAIL_PASSWORD).
      HIMALAYA_CONFIG: /app/config/himalaya/config.toml
      # OPENAI_API_KEY: set in .env.secrets
      # MCP_API_KEY: set in .env.secrets
      # ADMIN_API_KEY: set in .env.secrets (protects admin endpoints; falls back to MCP_BEARER_TOKEN)
      # SLACK_USER_TOKEN: set in .env.secrets (xoxp-... user token for channel cleanup — requires channels:history, chat:write scopes)
    ports:
      - "3002:3000"
    volumes:
      - ./config:/app/config:ro
      - admin_prewipe_backup:/backup/pre-wipe
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3000/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  workers:
    image: ghcr.io/davistroy/open-brain/workers:latest
    build:
      context: .
      target: workers
    container_name: open-brain-workers
    env_file:
      - .env.secrets
    environment:
      NODE_ENV: production
      POSTGRES_URL: postgresql://openbrain:${POSTGRES_PASSWORD}@postgres:5432/openbrain
      REDIS_URL: redis://redis:6379
      OPENAI_BASE_URL: https://api.openai.com/v1
      OLLAMA_URL: http://ollama:11434/v1
      CORE_API_URL: http://core-api:3000
      OPEN_BRAIN_API_URL: http://core-api:3000
      WIKI_REPO_URL: http://Gitea:3000/davistroy/open-brain-wiki.git
      WIKI_LOCAL_PATH: /tmp/open-brain-wiki
      GITEA_TOKEN: ${GITEA_TOKEN}
      NODE_OPTIONS: "--max-old-space-size=1200"
      # wiki-ingest makes git commits from inside this container; env vars
      # override lack of ~/.gitconfig and identify the bot author (A61).
      GIT_AUTHOR_NAME: "Open Brain Bot"
      GIT_AUTHOR_EMAIL: "bot@brain.troy-davis.com"
      GIT_COMMITTER_NAME: "Open Brain Bot"
      GIT_COMMITTER_EMAIL: "bot@brain.troy-davis.com"
      PIPELINE_USE_FLOWS: "true"  # DAG pipeline is now the only code path; kept for documentation
      # Outbound email primary path — himalaya CLI with TOML config (gitignored;
      # credentials sourced from Bitwarden BOND_EMAIL_PASSWORD). Weekly brief and
      # email-compose skill call HimalayaService.send().
      HIMALAYA_CONFIG: /app/config/himalaya/config.toml
      # COMPOSIO_API_KEY: set in .env.secrets (Composio MCP — calendar, email integrations)
      # OPENAI_API_KEY: set in .env.secrets
      # LLM_SPEND_URL: set in .env.secrets (optional — spend proxy for budget-check)
      # PUSHOVER_APP_TOKEN: set in .env.secrets
      # PUSHOVER_USER_KEY: set in .env.secrets
      # Optional nodemailer fallback for weekly-brief (Himalaya -> nodemailer -> Pushover chain).
      # Currently unset — delivery cascades directly from Himalaya to Pushover.
      # SMTP_HOST / SMTP_PORT / SMTP_USER / SMTP_PASS / SMTP_FROM: set in .env.secrets to enable
    volumes:
      - ./config:/app/config:ro
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  slack-bot:
    image: ghcr.io/davistroy/open-brain/slack-bot:latest
    build:
      context: .
      target: slack-bot
    container_name: open-brain-slack-bot
    env_file:
      - .env.secrets
    environment:
      NODE_ENV: production
      CORE_API_URL: http://core-api:3000
      REDIS_URL: redis://redis:6379
      OPENAI_BASE_URL: https://api.openai.com/v1
      NODE_OPTIONS: "--max-old-space-size=1200"
      # SLACK_BOT_TOKEN: set in .env.secrets   (xoxb-...)
      # SLACK_APP_TOKEN: set in .env.secrets   (xapp-...)
      # OPENAI_API_KEY: set in .env.secrets
    volumes:
      - ./config:/app/config:ro
    depends_on:
      core-api:
        condition: service_healthy
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  voice-pipecat:
    image: ghcr.io/davistroy/open-brain/voice-pipecat:latest
    build:
      context: packages/voice-pipecat
      dockerfile: Dockerfile
    container_name: open-brain-voice-pipecat
    env_file:
      - .env.secrets
    environment:
      # DEEPGRAM_API_KEY: set in .env.secrets
      # ANTHROPIC_API_KEY: set in .env.secrets
      REDIS_URL: redis://redis:6379
      CORE_API_URL: http://core-api:3000
    ports:
      - "8765:8765"   # WebSocket for Pipecat audio streams
      - "8766:8766"   # Health endpoint
    volumes:
      - ./config:/app/config:ro
    depends_on:
      redis:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8766/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    mem_limit: 4g
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  # Ollama runs as standalone container — connected via scripts/post-compose-up.sh
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: open-brain-ollama
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     - ollama_data:/root/.ollama
  #   healthcheck:
  #     test: ["CMD-SHELL", "ollama list || exit 1"]
  #     interval: 30s
  #     timeout: 10s
  #     retries: 5
  #     start_period: 30s
  #   mem_limit: 16g
  #   networks:
  #     - open-brain
  #   restart: unless-stopped

  file-ingestion:
    image: ghcr.io/davistroy/open-brain/file-ingestion:latest
    build:
      context: packages/file-ingestion
      dockerfile: Dockerfile
    container_name: open-brain-file-ingestion
    environment:
      PORT: "8080"
      LOG_LEVEL: "INFO"
    ports:
      - "8080:8080"
    volumes:
      - ${STAGING_DIR:-/mnt/user/openbrain/staging}:/data/staging:ro
    depends_on:
      core-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:8080/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    mem_limit: 1536m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  # --- Legacy voice services (kept for fallback until voice-pipecat is validated) ---

  faster-whisper:
    image: fedirz/faster-whisper-server:0.5.0-cpu
    container_name: open-brain-faster-whisper
    environment:
      WHISPER__MODEL: large-v3
      WHISPER__DEVICE: cpu
      WHISPER__COMPUTE_TYPE: int8
    ports:
      - "10300:8000"
    volumes:
      - whisper_model_cache:/root/.cache/huggingface
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health')"]
      interval: 30s
      timeout: 10s
      retries: 5
      start_period: 120s
    mem_limit: 8g
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  voice-capture:
    image: ghcr.io/davistroy/open-brain/voice-capture:latest
    build:
      context: .
      target: voice-capture
    container_name: open-brain-voice-capture
    env_file:
      - .env.secrets
    environment:
      NODE_ENV: production
      PORT: "3001"
      CORE_API_URL: http://core-api:3000
      WHISPER_URL: http://faster-whisper:8000
      OPENAI_BASE_URL: https://api.openai.com/v1
      NODE_OPTIONS: "--max-old-space-size=1200"
      # OPENAI_API_KEY: set in .env.secrets
      # PUSHOVER_TOKEN: set in .env.secrets
      # PUSHOVER_USER: set in .env.secrets
    ports:
      - "3001:3001"
    depends_on:
      core-api:
        condition: service_healthy
      faster-whisper:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3001/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  web-next:
    image: ghcr.io/davistroy/open-brain/web-next:latest
    build:
      context: .
      dockerfile: packages/web-next/Dockerfile
    container_name: open-brain-web-next
    environment:
      NODE_ENV: production
      PORT: "3001"
      HOSTNAME: "0.0.0.0"
      # TZ: ensures getHours() in RSC server code matches the user's timezone,
      # preventing SSR/CSR hydration mismatches (e.g. greeting, date headers).
      TZ: America/New_York
      # API_URL: used by next.config.ts rewrites (server-side, not browser-exposed)
      API_URL: http://core-api:3000
      # NEXT_PUBLIC_API_URL: browser-visible URL — intentionally empty in Docker;
      # all /api calls are proxied server-side via next.config.ts rewrites
      NEXT_PUBLIC_API_URL: http://core-api:3000
    ports:
      - "3003:3001"
    depends_on:
      core-api:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3001/dashboard || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    deploy:
      resources:
        limits:
          memory: 512m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  cloudflared:
    image: cloudflare/cloudflared:latest
    container_name: open-brain-cloudflared
    command: tunnel --no-autoupdate --config /etc/cloudflared/tunnel.yaml run
    environment:
      # CLOUDFLARE_TUNNEL_TOKEN: set in .env.secrets
      # Retrieve from Bitwarden before starting: bws secret get <open-brain-cloudflare-tunnel-token>
      TUNNEL_TOKEN: ${CLOUDFLARE_TUNNEL_TOKEN}
    volumes:
      - ./config/cloudflare/tunnel.yaml:/etc/cloudflared/tunnel.yaml:ro
    depends_on:
      core-api:
        condition: service_healthy
    mem_limit: 256m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  # Python batch-ingest sidecar — hosts financial-pipeline.py + utility-pipeline.py.
  # Long-lived container; host cron shells in with `docker exec` to run tasks.
  # Replaces the per-script deployment on open-brain-vm (G-C.0 decision: Python
  # sidecar on homeserver, not TS rewrite).
  financial-ingest:
    image: ghcr.io/davistroy/open-brain/ingest-sidecar:latest
    build:
      context: .
      dockerfile: docker/ingest-sidecar/Dockerfile
    container_name: open-brain-financial-ingest
    env_file:
      - .env.secrets
    environment:
      # BWS_ACCESS_TOKEN set in .env.secrets — required for bws secret list
      # INGEST_TRIGGER_SECRET set in .env.secrets — HMAC for core-api dispatch
      INGEST_SOURCE: financial  # binds trigger_server.py to financial-pipeline.py
      FINANCIAL_INBOX_DIR: /inbox
      FINANCIAL_PIPE_DIR: /data
      FINANCIAL_CONFIG_DIR: /app/config/financial
      # Post captures to core-api over the internal Docker network, not via
      # the public brain.troy-davis.com URL. Public URL is fronted by
      # Cloudflare Access which 302s unauthenticated POSTs.
      CAPTURE_API_URL: http://core-api:3000/api/v1/captures
      CAPTURE_API_CALLER: financial-pipeline
      TZ: America/New_York
    volumes:
      # Host inbox — drop CSVs here and they'll be processed on next cron run.
      # Matches backup-path convention under /mnt/user/appdata on Unraid host.
      - /mnt/user/appdata/open-brain/financial-inbox:/inbox
      # SQLite DB + any future sidecar state
      - financial_ingest_data:/data
      # Config (plaid-config.yaml, merchants.yaml) read-only
      - ./config:/app/config:ro
    depends_on:
      core-api:
        condition: service_healthy
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:

  # Utility batch-ingest sidecar — same image as financial-ingest, different
  # config + inbox. Handles utility-pipeline.py (gas, power, water summaries).
  # Build block is duplicated so either service can rebuild the shared image.
  # ── Observability stack ──────────────────────────────────────────────────────
  # Start with: docker compose --profile observability up -d
  # Prerequisite: Loki Docker log driver plugin installed once on the Docker host:
  #   docker plugin install grafana/loki-docker-driver:latest --alias loki --grant-all-permissions
  # See docs/runbooks/observability.md for bring-up, cutover, and data migration steps.

  loki:
    image: grafana/loki:latest
    container_name: open-brain-loki
    volumes:
      - loki_data:/loki/data
      - ./config/loki/loki-config.yaml:/etc/loki/local-config.yaml:ro
    ports:
      - "3100:3100"
    command: -config.file=/etc/loki/local-config.yaml
    mem_limit: 512m
    networks:
      - open-brain
    restart: unless-stopped
    profiles:
      - observability
    # No in-container healthcheck: grafana/loki:latest is a scratch-based image
    # with no shell and no wget — any CMD-SHELL probe fails with "executable not
    # found" and Docker marks the container unhealthy forever, even when Loki is
    # responding 200 on /ready. A `loki -version` healthcheck would always pass
    # (worse — masks real failures). External monitoring: prometheus scrapes
    # Loki, grafana datasource UI surfaces query problems. See issue #218.
    # NOTE: No logging driver on loki itself — prevents chicken-and-egg startup failure.

  pushgateway:
    image: prom/pushgateway:latest
    container_name: open-brain-pushgateway
    ports:
      - "9091:9091"
    mem_limit: 128m
    networks:
      - open-brain
    restart: unless-stopped
    profiles:
      - observability
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9091/-/healthy || exit 1"]
      interval: 30s
      timeout: 5s
      retries: 3

  prometheus:
    image: prom/prometheus:latest
    container_name: open-brain-prometheus
    volumes:
      - ./config/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - ./config/prometheus/alerts:/etc/prometheus/alerts:ro
      - prometheus_data:/prometheus
    ports:
      - "9090:9090"
    command:
      - '--config.file=/etc/prometheus/prometheus.yml'
      - '--storage.tsdb.path=/prometheus'
      - '--web.enable-lifecycle'
    mem_limit: 1g
    networks:
      - open-brain
    restart: unless-stopped
    profiles:
      - observability
    depends_on:
      pushgateway:
        condition: service_healthy
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:9090/-/healthy || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  grafana:
    image: grafana/grafana:latest
    container_name: open-brain-grafana
    environment:
      GF_SECURITY_ADMIN_PASSWORD: ${GRAFANA_ADMIN_PASSWORD:-admin}
      GF_USERS_ALLOW_SIGN_UP: "false"
      GF_SERVER_HTTP_PORT: "3050"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./config/grafana/provisioning:/etc/grafana/provisioning:ro
      - ./config/grafana/dashboards:/var/lib/grafana/dashboards/open-brain:ro
    ports:
      - "3050:3050"
    mem_limit: 512m
    networks:
      - open-brain
    restart: unless-stopped
    profiles:
      - observability
    depends_on:
      prometheus:
        condition: service_healthy
      loki:
        # Loki has no in-container healthcheck (scratch-based image, see #218).
        # service_started is the most we can wait for; restart: unless-stopped
        # handles crash recovery, prometheus scraping handles ongoing health.
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -qO- http://127.0.0.1:3050/api/health || exit 1"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s

  utility-ingest:
    image: ghcr.io/davistroy/open-brain/ingest-sidecar:latest
    build:
      context: .
      dockerfile: docker/ingest-sidecar/Dockerfile
    container_name: open-brain-utility-ingest
    env_file:
      - .env.secrets
    environment:
      # INGEST_TRIGGER_SECRET set in .env.secrets — HMAC for core-api dispatch
      INGEST_SOURCE: utility  # binds trigger_server.py to utility-pipeline.py
      UTILITY_INBOX_DIR: /inbox
      UTILITY_PIPE_DIR: /data
      UTILITY_CONFIG_DIR: /app/config/utility
      CAPTURE_API_URL: http://core-api:3000/api/v1/captures
      CAPTURE_API_CALLER: utility-pipeline
      TZ: America/New_York
    volumes:
      - /mnt/user/appdata/open-brain/utility-inbox:/inbox
      - utility_ingest_data:/data
      - ./config:/app/config:ro
    depends_on:
      core-api:
        condition: service_healthy
    mem_limit: 1500m
    networks:
      - open-brain
    restart: unless-stopped
    logging:
      driver: loki
      options:
        loki-url: "${LOKI_URL:-http://loki:3100/loki/api/v1/push}"
        loki-batch-size: "400"
        loki-retries: "3"
        loki-max-backoff: "800ms"
        loki-timeout: "2s"
        loki-pipeline-stages: |
          - json:
              expressions:
                level: level
                name: name
                msg: msg
          - labels:
              level:
              name:
