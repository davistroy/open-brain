# Ultra-Plan Analysis — Arch-Review v5 Remediation (2026-07-12)

Phase 0–4 output of `/ultra-plan based on all of the findings`. Investigation performed by 6 parallel sub-agents against HEAD `cd14c1f`; all file:line refs verified 2026-07-12. This document is the requirements input for plan generation.

## Phase 0 — Constraints (from CLAUDE.md; all gate every change set)

1. **Tests:** raise coverage, never lower thresholds; workers floor 78/81 + four per-file 100% locks; `lint`=tsc incl. test files; lockfile committed with package.json; `pool:'forks'` with min/maxForks.
2. **Deploy:** manual surgical only (`pull` + `up -d --force-recreate --no-deps <svcs>`); two-gate config-diff before compose changes; NEVER `--remove-orphans` or bare `up -d`; Unraid host has no python3.
3. **Schema:** no auto-migration; additive `00NN_*.sql` + `regenerate-init-schema.sh` + commit both; parity CI; ledger via migrate-manual.sh.
4. **Security:** secrets in Bitwarden only; 3-step lockstep for new secrets; BYPASS_CALLERS lockstep; NODE_ENV fail-closed; admin_audit invariants (TRUNCATE + retention exclusion, code-asserted).
5. **Observability:** shared stack is external (ADR-0004); cron slots must pass scheduler-slots.test.ts; Pushover app-layer alerts.
6. **DoD:** LAB_NOTEBOOK entry before first commit; PR to protected main; required checks green; `Refs #N` for partial closure.
7. **Cost:** T0→T3 tiering; no per-item LLM calls; <$35/mo. **Memory:** 1.5 GB RSS ceiling. **Skills:** never override BaseSkill.execute(); concurrency 2 default.

## Phase 1 — Key investigation findings (per-cluster highlights; full tables in session record)

**Data/retention (DA):**
- DA-1: constraint name exactly `briefs_source_skill_log_id_fkey` (init-schema.sql:1816); column nullable; partial unique index `WHERE source_skill_log_id IS NOT NULL` → SET NULL safe. Migration auto-discovered by glob (migrate-manual.sh:60,109-116) — dropping `0036_*.sql` is sufficient; must be idempotent under ON_ERROR_STOP=1 (`DROP CONSTRAINT IF EXISTS`). Fold in `DROP FUNCTION IF EXISTS public.fts_search(text, integer)` (DA-7: dead, defined only in the snapshot at init-schema.sql:130-133; live functions are fts_only_search/hybrid_search — do not touch). Loop refactor must preserve: admin_audit exclusion invariant tests (test:53-66,192-196), parameterized table_name, sql.raw() from static config only; call-count assertions (test:73,168) move in lockstep. Test pattern: PgDialect.sqlToQuery + makeMockDb, reject only the skills_log DELETE.
- DA-2: only settings.ts:15-38 needs the GET/PUT whitelist split — workers hydrate tokens via direct Drizzle (gmail-client.ts:508-528, hotmail-client.ts:57-71), frontend never touches token keys, no HTTP seeding path exists. Preserve `{value:null}` contract + ValidationError shape (settings-routes.test.ts:93-149).
- DA-3/RC-15: policy expansion (container_health, email_classifications, voice_sessions, retention_audit self-prune, captures deleted_at>90d w/ backup-age precondition) — all count-based test assertions (toHaveLength(5), *2 calls) move in lockstep; verify each timestamp column against init-schema first.
- DA-9: only skill-execution.ts:32 has removeOnFail age; ~9 other queue files count-only.
- DA-5 (partial HNSW + iterative_scan + hard purge) and DA-6 (encryption at rest) = flagged separate/deferred work.

**Platform (PLT/PE-platform):**
- deploy.md rewrite map: Inventory:27-30 (4 GPL containers → external), §2:65-88 + §3:92-140 (add digest-verified rollback anchors — Entry 183 :12665,:12672 — tag strings insufficient, slack-bot mis-guess proven), §4:144-177 (migration via container, host has no psql; add config-diff gate + --remove-orphans prohibition), §5:181-210 (replace wholesale — :192 `cat >` override, :205 `rm` override), §7:235-259 (retired profile), §8:263-269 (:267 false postgres_data claim).
- slo-alert.md: 3 alerts (slo.yml:79,102,124), template = capture-flow-alert.md (condition table → numbered diagnosis w/ exact commands → per-mode mitigation → related).
- Backup dead-man's switch: backup.sh:198 writes manifest.json, :231 updates `latest` symlink → freshness = latest/manifest.json mtime. Wiring: (1) workers ro-mount `/mnt/user/backup/openbrain/latest:/backup-latest:ro` (compose :187 area); (2) pipeline-health stat + `openbrain_backup_age_seconds` gauge via existing push-metrics (:74 default pushgateway:9091, reachable — compose :206); (3) new alerts/backup.yml rule >93600s + runbook; (4) Pushover branch in sendAlert (:417) for independence from PLT-H2's unproven delivery. Avoids the pushgateway-host-reachability unknown.
- doc-sync (ci.yml:227-248): version-strings only (sync-docs.sh checks package.json/PRD/README/CHANGELOG semver); promotion checklist embedded at :230-234; structurally blind to procedure drift — promote AND note the gap.
- build-images.yml: no CI gate, no failure alert; 8 build-push steps; intersects #242 (build-push-action 6→7, touches all steps) + #239/240/241/236. Cheap fix: failure-notification job + merge-watch protocol; optional workflow_run gate on CI.
- dependabot.yml: npm×3 + github-actions only. pip needs 8 directories; docker needs 6 Dockerfiles; compose third-party pins not covered (manual note).
- container-health.ts:53-61: probes dead `litellm:4000`, omits faster-whisper, stale web comment.
- RC-19 mechanism: monthly-audit.yml (cron 1st 10:00, Slack Block Kit path exists :87-131) can surface a checked-in register; `operational-followups.md` DOES NOT EXIST — real register is LAB_NOTEBOOK:148 Action Items table (right schema, wrong visibility). Best reminder host: secret-rotation skill (monthly, Pushover, shares RC-13 BWS dependency).
- RC-13: BWS_ACCESS_TOKEN is the bootstrap credential — must NOT go in secrets-map resolver (chicken-and-egg; load-secrets would exit 2); correct fix = template entry (hand-populated) + workers compose env.

**Voice/security (SEC/IA):**
- SEC-A1: NOTHING connects to ws://8765 in prod (exhaustive grep + D50/D51/D121/A81: iOS Shortcut is HTTP→:3001; mobile is batch upload; streaming is deferred A81; only intended consumer = never-run manual soak test P24). Options: (a) loopback bind :259-260 (+8766), (b) compose `profiles: [voice-streaming]` gate, (c) token handshake in main.py:59-108 (WS auth awkward for Shortcuts/browsers). (a)/(b) break nothing today.
- Voice Bearer phase 2: purely operator action, zero code — enforcement (server.ts:75-87 timing-safe), proxy forwarding (voice-captures.ts:40-42), env_file wiring, secrets-map + template entries all exist. Order: BWS secret → iOS Shortcut header → mobile EXPO_PUBLIC_VOICE_SECRET → load-secrets → restart voice-capture AND core-api → smoke (401 without / 2xx with). Clients FIRST or all direct captures 401 (runbook voice-capture-auth.md:27 accurate).
- IA-M1: ingest.ts:79-83 throws on all 4xx; spool retries forever (transcript-spool.ts:86-89). Fix: 409 = terminal success (mirror slack-bot core-api-client.ts:44-50,94-96,129-149 okStatuses pattern; tolerate missing id in 409 body) + max-age dead-letter w/ Pushover for other stuck files (parallels corrupt-file discard :75-78). Only 409 is safe-terminal — not other 4xx.
- SEC-B1: `@hono/node-server ^1.19.11` in core-api + voice-capture package.json; caret already permits 1.19.13 → `pnpm update` + floor bump; no code change.
- SEC-A2 topology resolved: mobile → brain.troy-davis.com → tunnel.yaml:15-16 → web-next:3001 → proxy.ts:19-21 overwrites caller → Bearer never validated. Option 1 = new tunnel hostname (e.g. api.brain.troy-davis.com → core-api:3000) + config.ts:1 repoint + CF Access on new hostname (must preserve isInternalIp defense; matcher exclusion impossible — same /api/v1 paths). Option 2 = delete mobile-auth.ts + app.ts:161-166 registrations + 3 test files + secrets-map/template MOBILE_API_KEY + simplify rate-limit mobile tier. CRITICAL unknown U3: is CF Access actually enforced for the native app on brain.troy-davis.com? If mobile works today WITHOUT a CF Access service token, the mobile surface currently has NO auth control → Option 1 becomes strongly preferred.
- SEC-A6: node:22-alpine ships `node` user. Safe now: core-api/slack-bot/web-next (no writable volumes). voice-capture needs spool-dir chown before USER. ingest-sidecar needs host-UID coordination (bind mounts) — last.

**QA:**
- QA-1 catch-up sizing: scheduler.ts 0/307 (mock bullmq Queue only, ~0.5d); skill-execution.ts 2/486 (Worker closure + 26-case switch — hardest, ~1-1.5d); ingest-process.ts 0/202 (mock db+fetch; `processIngestProcessJob` exposed for unit testing, ~0.5-0.75d); **src/skills/**memory-consolidation-query.ts 8/170 (buildClusters pure union-find cheap; invert existing vi.mock, ~0.5d). Deficit 493 lines of 1,155 uncovered in these four. Total ~2.5-3.5 dev-days. HARD ORDERING: `--coverage` flag added to workers test script only after local run ≥78% — else build-and-test (required check) blocks all PRs instantly.
- QA-9: drift-monitor.test.ts:724 + weekly-brief.test.ts:261 `toBeGreaterThan(0)` → `toBeGreaterThanOrEqual(0)` (correct form exists at weekly-brief:634).
- QA-13: 7 sites exact (mcp-activity-logger.test.ts:212,233,253,271; voice-session-service.test.ts:152,250; run-agent.test.ts:574).
- QA-5: setup.ts:166-168 zero-vector stub; fix = committed fixture generated from the REAL embedder (not fabricated).
- QA-7: email-worker pure functions are module-private — choose exporting them (simple, deploy-critical file touch) or handler-level tests w/ mocked fetch/PostalMime; new ci.yml job (npm-based, outside pnpm workspace): `npm ci && tsc --noEmit && vitest run`; ~10 cases (isTransientStatus, isSenderAllowed ×4, allowlist parse, base-URL derivation ×2). Both CF dirs also need `.npmrc` `legacy-peer-deps=true` (SW5-L13). MUST land before merging #235/#237/#238.
- QA-4: exact check names "Validate init-schema.sql" (ci.yml:127-128) + "Python lint & typecheck" (:146-147); promote via `gh api .../branches/main/protection` (Phase 5b precedent, keep enforce_admins=false/strict=false); confirm 2 green runs first.
- QA-2: ingest-e2e.test.ts:50 INGEST_E2E gate; ci.yml:211-217 explicit deferral; needs test-core-api + test-workers services added to docker-compose.test.yml (sidecar already targets test-core-api:3000).
- QA-3: web-next has FULL test infra installed (RTL 16, jest-dom, user-event, msw 2, jsdom) — vitest include glob scoped to lib only; broaden it (KEEP .next exclusion), add top-page component tests; Playwright smoke = tests/smoke/quick-capture.spec.ts, self-skips without live stack, wired to no workflow — ride QA-2 stack.
- QA-15: stale 85.57% in CLAUDE.md:188, IMPLEMENTATION_PLAN.md:60/243, CHANGELOG.md:18 (true: 81.52%); stale "Vitest 2.x feature" comment at workers/vitest.config.ts:27 (NOT core-api). LAB_NOTEBOOK historical entries left as-is (audit trail).
- QA-14: shared + slack-bot have NO vitest.config at all; voice-capture has reporter block, no thresholds; web-next test:coverage invoked by nothing. Measure first, exclude test globs explicitly (QA-15 lesson), gate later.

**Runtime (SW/PE-perf/IA/SA):**
- runAgent (#204): loop in shared/src/services/run-agent.ts — tool results appended untruncated at :423 (:414-419); usage available via accumulateUsage(:176-190,:339); estimateTokens() exists (utils/tokens.ts:6). 5 callers: monthly-reflection (UNBOUNDED — 200 full captures/call at :164), wiki-ingest (UNBOUNDED — full page content at :97), wiki-lint/email-compose/email-compose-assist (bounded .slice(0,300)). Design: (1) per-tool-result char cap 8-16KB w/ truncation marker at :395-419 (all skills inherit); (2) cumulative input_tokens budget (~150K) w/ early stop ON ITERATION BOUNDARY (:270 guard, checked after :339) + synthetic "summarize now" turn; (3) monthly-reflection per-capture truncation 300-500 chars at :164 (email-compose precedent 300). run-agent.test.ts (875 lines, 30 cases) has NO truncation/budget tests — add. Anthropic-only enforcement at :238 preserved.
- PE-M1: offset .max(450) (not 490 — limit.max is 50; bound offset+limit≤500 → match_count≤500 → CTE scan ≤2000). MCP path already safe (no offset param).
- PE-M2: embedBatch (embedding.ts:196) truncation is NON-adaptive (:200 fixed 16K slice) + all-or-nothing (:212-232) — naive wiring converts single-chunk failures into whole-document failures. Prerequisite: adaptive truncation in batch OR per-chunk fallback on batch failure. Production doc path already fans chunks to individual jobs (document-pipeline.ts:280-299); :304 is the inline fallback path only. Scope: harden embedBatch + wire inline path; batch-job restructure = flagged out.
- #217: BullMQ ^5, legacy .add({repeat}) API at 17 sites — reconcile with getRepeatableJobs()+removeRepeatableByKey() (NOT JobScheduler API), per-queue (5 queues), AFTER registration, exact-key equality. Constraint: scheduler-slots.test.ts regex needs ≥15 literal `const *Cron = '...'` declarations — don't refactor them away.
- IA-L6: createTransport (email.ts:63-71) — add connectionTimeout/greetingTimeout/socketTimeout: 15_000 (repo convention: budget-check.ts:33, llm-gateway.ts:560, 8 skills).
- SA-5 CORRECTED: load() already runs throwing validateAiRoutingConfig (loader.ts:82; rules at ai-config-schema.ts:59-79); validateTaskRouting (warn-only) is redundant there. REAL gap: reload() (loader.ts:115) never runs the throwing validator — fix = run validateAiRoutingConfig in reload() with errors collected into ReloadResult (reject the reload, keep old config, don't crash). Tier-capability tags = flagged broader refactor.
- IA-M4: LLM gateway = 3 instrument points (llm-gateway.ts:487,555,709) covering OpenAI chat + openai_compat + Anthropic; embeddings BYPASS gateway (embedding.ts:80,150,204) — separate wrap; Deepgram/Pushover/Composio separate. Workers has NO prom registry (Pushgateway-only via push-metrics) — shared histogram module registering per-process; labels {provider, operation, status_class} (no URL labels).
- IA-M3: NO response zod schemas exist anywhere (schemas/ = 2 request-only files); generated-OpenAPI without authoring response schemas first would not cover the drift. Lighter win: shared response-types module for slack-bot shims (core-api-client.ts:174-176 items→captures; :277-280 items→entities + mention_count→capture_count + entity_type→type). web-next/mobile deliberately don't import shared — keep their drift tests.
- SW5 smalls verified: redis .on('error') missing (main.ts:162,175); hardcoded Slack DM fallback (skill-execution.ts:261); stale skill-count comments (:13,:41 vs 22 actual); Pushover env duality (voice-capture PUSHOVER_TOKEN/USER vs shared PUSHOVER_APP_TOKEN/USER_KEY); hotmail console.log (:161); pnpm.overrides 5 unscoped exact pins; parseUUIDParam in only 2 of 28 route files (38 raw sites); XFF first-hop trust + lax IPv6 regexes (rate-limit.ts:246,187,191).

**Docs/governance:**
- SA-6 sweep list: TDD.md:4035,1918,3837-3838 (+:17,:4454 historical) auto-migration claims — mirror CLAUDE.md:32 ledger model verbatim; README.md:222 "17 containers"→13+external, :35 observability rows, :69-70 v0.6→v0.7; PRD.md:1532,1893 + TDD.md:3984 retired `--profile observability`; ADR-0003 status Proposed→Accepted; PRD.md:1531 nonexistent "circuit breaker on external API calls" reword; SA-11 compose comment contradiction (:412-414).
- #226 closeable: fix = PR #230/1710c54 pgUuidArray at daily-connections-query.ts:134, proven live Entry 180; note attribution nuance (issue says core-api; fix in workers daily-connections — same query/error/timing).
- CHANGELOG [Unreleased] missing everything since 2026-07-01 (ADR-0004/#231, #230, Dependabot waves #232-234, grouped-updates config). OPEN_ITEMS.md:7 stale ("Waves 3-4 remain" false).

## Phase 2 — Interaction Map

**Same-root-cause groups (solve together):**
- G1: DA-1 = SW5-H3 = SA-14 (mechanical) + RC-19 (systemic) — mechanical fix in CS-A; forcing function in CS-D.
- G2: QA-1 = SW5-H1 = SA-9/PE-M6(platform) — CS-F.
- G3: #204 = SW5-H2 = PE-H1(perf) = SA-10 — CS-E.
- G4: #217 = PE-M3(perf) = IA-M5 — CS-E.
- G5: Doc drift: PLT-C1 + PLT-H1 + PLT-H3 + SA-6 + SA-12 + PE-L2/L5 + QA-15-docs — CS-B; meta-cause (doc-sync observe-only) — CS-G.
- G6: Voice acceptance gap: SEC-A1 + SEC-A3/IA-M2/RC-11 + IA-M1 — CS-C.

**Shared-file collision points (sequencing constraints):**
- `data-retention-prune.ts`: CS-A (isolation) then CS-H (policy expansion) — CS-A first, CS-H rebases; count-based tests move each time.
- `ci.yml`: CS-F (email-worker job), CS-G (alert-rules job, doc-sync promotion), CS-H (e2e) — small sequential PRs.
- Branch protection (gh api): CS-F required-checks + CS-G doc-sync promotion — coordinate in one PATCH each time.
- `docker-compose.yml`: CS-C pipecat gating + CS-G workers backup-mount (+ optional SA-1 bind parameterization) — BATCH into ONE config-diff-gated deploy window; postgres/redis untouched.
- Workers coverage arithmetic: CS-A tests + CS-E tests (monthly-reflection/scheduler changes) + CS-F catch-up all raise %; CS-F's --coverage flag is the barrier — land last.
- deploy.md: CS-B rewrite includes CS-G's digest-check content — one document PR.
- Dependabot PRs #235-243: #235/237/238 (CF workers) blocked on CS-F email-worker CI; #239-242 (actions majors) merged one-at-a-time under CS-G watch protocol; #243 (npm minor-patch) safe after CS-F.
- `deploy/.env.secrets.template`: CS-D adds BWS_ACCESS_TOKEN; CS-H (mobile Option 2) may remove MOBILE_API_KEY — no conflict, note.
- runAgent (shared pkg): CS-E change affects 5 skills in workers+core-api — rebuild shared, run both suites.

**Change-to-system:** migration 0036 → parity CI + prod ledger deploy (workers-only recreate). Compose window → config-diff gates, no data-store recreate. Required-check changes → repo settings (reversible via gh api). secret-rotation extension → new file read (OPERATOR_ACTIONS.md) — must not fail the skill when file absent.

## Phase 3 — Change Sets (solution design summary)

**CS-A — Retention integrity hotfix [Immediate; blocks: nothing; ~half-day + operator]**
0036_briefs_fk_and_cleanup.sql (DROP CONSTRAINT IF EXISTS + re-ADD with ON DELETE SET NULL; DROP FUNCTION IF EXISTS fts_search(text,integer)) → regenerate-init-schema.sh → commit both. Per-entry try/catch in pruneRetentionData: record per-table success/failure audit row, continue loop, throw aggregate at end if any failed (alert semantics preserved — never swallow). Regression test: reject only skills_log DELETE, assert 4 tables pruned + failure surfaced; keep admin_audit invariant tests green; update call-count assertions deliberately. Operator: deploy (migrate container per CLAUDE.md mechanics; simple ALTER — no PGOPTIONS needed), then RI-A: check retention_audit for 07-05/07-12 gaps, clear 2 failed BullMQ jobs.
*Trade-off:* SET NULL over CASCADE (briefs are user-facing artifacts; provenance loss acceptable, data loss not). *Rollback:* revert PR; reverse migration re-adds bare FK.
*Verify:* `pnpm --filter @open-brain/workers exec vitest run src/__tests__/data-retention-prune.test.ts` · `bash scripts/validate-init-schema.sh` (CI) · post-deploy `migrate-manual.sh --status` shows 0036 · next Sunday retention_audit has 5 rows.

**CS-B — Runbook & docs truth [Immediate; docs-only; ~1 day]**
deploy.md full rewrite per §-map (Entry 183 procedure, digest anchors, config-diff gate, prohibitions, container-based migration, external observability, corrected §8). observability.md rewrite; slo-alert.md authored (3 alerts, capture-flow template); web-rollback.md fixed or deleted (+deploy.md refs); post-compose-up.sh header + container-health/web comments (PE-L2). SA-6 sweep (TDD ×4 auto-migration lines mirroring CLAUDE.md:32; README counts/versions; PRD profile refs; ADR-0003→Accepted; PRD:1531 reword; SA-11 comment). QA-15 figures (CLAUDE.md:188, CHANGELOG:18 → 81.52%; old IMPLEMENTATION_PLAN annotated not silently edited). Cleanup: .gitignore `._*`; close #226 (cite 1710c54 + attribution nuance); CHANGELOG [Unreleased] refresh; OPEN_ITEMS reconcile.
*Rollback:* git revert. *Verify:* `grep -n 'cat >\|rm .*override' docs/runbooks/deploy.md` → 0 hits · `grep -rn 'profile observability' docs/ scripts/` → 0 actionable hits · `bash scripts/sync-docs.sh` green · `ls docs/runbooks/slo-alert.md`.

**CS-C — Voice & edge hardening [This week; small code + operator]**
voice-pipecat gating per owner decision (recommend compose `profiles: [voice-streaming]` — nothing connects today; loopback bind acceptable alternate; either also covers 8766) + ADR-0002 port table + SECURITY.md. IA-M1: 409-as-terminal-success in ingest.ts (tolerate missing id) + max-age dead-letter w/ Pushover in transcript-spool + tests. IA-L1: AbortSignal.timeout(150_000) on voice proxy upstream fetch. SEC-B1: @hono/node-server →^1.19.13 (both packages, lockfile). Operator (register item): voice Bearer rollout, clients-first order per runbook.
*Rollback:* profile gate = remove one line; 409 fix = revert. *Verify:* `pnpm --filter @open-brain/voice-capture test` · compose config diff shows only pipecat delta · post-rollout smoke 401/2xx per runbook.

**CS-D — Governance forcing function [This week; small]**
RC-13: BWS_ACCESS_TOKEN → template (bootstrap exception, NOT secrets-map resolver) + workers compose env; operator provisions. OPERATOR_ACTIONS.md: machine-readable dated register (ID | Action | Due | Owner | Source | Status), seeded from live items (A131 verify, voice Bearer, RC-10 decision, Gmail re-consent, RI-3 quarterly, shm_size next-restart-window, CS-A prod verification if pending). secret-rotation skill: parse register, Pushover on overdue/approaching items (graceful when file absent); monthly-audit.yml step: render register into GITHUB_STEP_SUMMARY + existing Slack block.
*Trade-off:* extend secret-rotation vs new skill — reuse wins (monthly cadence + Pushover + shared BWS dependency). *Verify:* `pnpm --filter @open-brain/workers exec vitest run src/skills/__tests__/secret-rotation*` · manual `workflow_dispatch` of monthly-audit shows the register.

**CS-E — Agent & runtime guardrails [30d; ~2-3 days]**
runAgent: per-tool-result cap (12KB default, marker) + cumulative input_tokens budget (150K default, override per skill) + iteration-boundary early stop w/ synthetic summarize turn + tests. monthly-reflection :164 per-capture truncation (400 chars). Closes #204. PE-M1 offset .max(450) + schema test. #217 legacy-API reconciliation (per-queue, exact key, post-registration, preserve const *Cron literals) + test. IA-L6 SMTP 15s timeouts. SA-5: validateAiRoutingConfig in reload() with errors→ReloadResult (reject reload, keep old config). embedBatch: add adaptive-truncation/per-chunk-fallback semantics, wire inline document path (batch-job restructure flagged out). SW5 smalls: redis error listeners; MORNING_BRIEF_SLACK_CHANNEL unset → warn+skip (not throw — availability over strictness for a solo notification); comment counts; Pushover env unification (adopt shared names in voice-capture, keep old as deprecated fallback one release); hotmail console.log→logger.
*Risk:* cap too tight degrades wiki-ingest/reflections — make both knobs per-call options with defaults; never truncate mid-toolResults assembly. *Verify:* `pnpm --filter @open-brain/shared test` (run-agent suite + new budget cases) · `pnpm --filter @open-brain/workers test` · schema test rejects offset 451.

**CS-F — Test the spine, arm the gates [30d; ~3-4 days; ordered]**
(1) QA-9 flake one-liners + QA-13 sleep conversions. (2) Catch-up: scheduler.ts → ingest-process.ts → memory-consolidation-query.ts → skill-execution.ts (patterns per investigation; imitate data-retention-prune.test.ts + scheduler-slots). (3) BARRIER: local `vitest run --coverage` ≥78 lines → add --coverage to workers test script (+ workers vitest.config.ts:27 comment fix). (4) QA-5 fixture (generated from real embedder, committed). (5) QA-7 email-worker: export pure functions + ~10-case vitest + tsc --noEmit + new ci.yml job + .npmrc both CF dirs → THEN merge #235/#237/#238. (6) QA-4: promote "Validate init-schema.sql" + "Python lint & typecheck" via gh api (2-green-run precondition; keep enforce_admins/strict as-is).
*Risk:* flag-first bricks all PRs (hard ordering); skill-execution mock complexity may run to high estimate. *Verify:* `pnpm --filter @open-brain/workers exec vitest run --coverage` ≥78/81 · `gh api .../protection --jq '.required_status_checks.contexts'` shows 4 · email-worker job green.

**CS-G — Automation & alerting gates [30d; ~1-2 days + one live-host session]**
build-images failure-alert job (+optional workflow_run gate); merge #239-242 one-at-a-time w/ post-merge watch. dependabot.yml += pip (8 dirs) + docker (6 Dockerfiles) + compose-pins manual note. validate-alert-rules.sh CI job. doc-sync promotion per its checklist (noting version-only scope). container-health: drop litellm, add faster-whisper real health path. Backup dead-man's switch: workers ro-mount + openbrain_backup_age_seconds in pipeline-health + alerts/backup.yml + runbook + Pushover branch. LIVE-HOST SESSION (operator+assistant): verify A131 logs; test WorkersMetricsAbsent delivery (PLT-H2); confirm/clear CS-A jobs — one SSH session closes 3 RIs. COMPOSE WINDOW: batch workers-mount + CS-C pipecat gating (+ SA-1 bind parameterization if desired) through the two-gate config-diff procedure.
*Verify:* intentionally-broken rule file fails CI locally · `curl pushgateway:9091/metrics | grep backup_age` post-deploy · dependabot PRs appear for pip/docker.

**CS-H — Structural (90d)**
QA-2 full-stack test compose (test-core-api + test-workers; INGEST_E2E=1) → QA-3 web-next (broaden include, keep .next exclusion, top-page RTL+msw tests, wire smoke, QA-11 a11y). SEC-A2 per owner decision + ADR-0005 (verify U3 first: does the native app pass CF Access?). IA-M3 response zod schemas → shared response-types (slack-bot scope; web-next/mobile keep drift tests). IA-M4 outbound metrics (gateway 3 sites + embedding wrap; shared histogram module; pushgateway payload for workers). DA-2 settings GET/PUT split + tests. DA-3+RC-15 retention expansion (windows decided; captures purge gated on backup-age precondition). DA-4 TTS size guard; DA-9 removeOnFail age rollout; SW5-M2 parseUUIDParam rollout; SW5-M1 XFF/IPv6 tighten; SW5-L14 override annotations. SEC-A6 USER staged (safe trio → voice-capture w/ chown → sidecar w/ host UID). QA-14 measure-then-arm remaining packages. PE-M5 bare-metal runbook; PE-L1 .env.example refresh; SA-3 declarative external-network join; SA-7 healthcheck static route; SA-8 dev portability.

## Phase 4 — Report elements

**Compliance check:** all designs comply with Phase 0 constraints. Two deliberate touchpoints flagged: CS-F modifies required checks (constitution-consistent — adds gates); CS-A changes test call-count assertions (deliberate, documented). No constraint conflicts found.

**Duplicates/misdiagnoses discovered:** SA-5 as written was half-wrong (load() already fail-fast; gap is reload()); v4 PE-M2 wrong (embedBatch exists); "operational-followups.md" doesn't exist; offset cap 490 wouldn't bound the sum (450 does); QA-1 4th file path corrected; QA-3 "no infra" actually "infra unused"; #226 attribution nuance (core-api vs workers).

**Unknowns register:** U1 (High): did prod prune fail 07-05/07-12? — resolve in CS-A operator step before assuming; affects only messaging, not the fix. U2 (resolved by design): pushgateway host-reachability — avoided via workers mount. U3 (High, gates CS-H mobile): CF Access enforcement for the native app on brain.troy-davis.com — verify in CS-G live-host session or via CF dashboard BEFORE the SEC-A2 decision executes. U4 (Med): A131 backup runs — CS-G session. U5 (Low): skill-execution mock complexity. U6 (Low): dependabot pip granularity (directories vs `directories:` list syntax).

**Risk highlights:** CS-A: CASCADE-instead-of-SET-NULL = data loss (design pins SET NULL); swallowing errors in try/catch = hides failures (design: fail-at-end). CS-F: flag-before-tests bricks merges (barrier explicit). CS-C: server-secret-before-clients 401s all captures (order pinned). CS-G: workflow_run gate misconfig stops publishes (alert-job first, gate optional). CS-E: over-tight caps degrade agents (per-call knobs).

**Scope boundaries — NOT covered (flagged separately):** DA-5 partial-HNSW rebuild + hard-purge (needs benchmark + CONCURRENTLY window); DA-6 encryption at rest (owner decision); PE-M4 env_file splitting (fail-open risk, own design pass); deploy.sh full scripting (PE-M1-platform; drift risk — revisit after CS-B/G stabilize); QA-8 perf gate; QA-12 full ESLint rollout (ratchet only, opportunistic); RC-16/17/18 (household-scale accepts, revisit with RC-15); #73 Qdrant / #72 GPU / #54/#57 voice architecture / #196 mobile scope (tracked deferrals); voice-pipecat token handshake (only if owner picks option c).

**Owner decisions — RESOLVED 2026-07-12 (interactive):**
1. **SEC-A1 = LEAVE AS-IS (explicit risk acceptance).** No gating/bind change ships. CS-C's pipecat code item is REPLACED by a documentation item: record the acceptance in ADR-0002's port table, SECURITY.md, the arch-review risk-acceptance register, and Decision Log (D135) — so future reviews see a decided acceptance, not an open finding. The CS-G compose window now carries only the workers backup-mount (+ optional SA-1).
2. **SEC-A2 = VERIFY CF ACCESS FIRST, then decide.** CS-H carries: (a) a verification work item (does the native app pass CF Access on brain.troy-davis.com? — CF dashboard check or CS-G live-host session), (b) both option branches pre-designed, (c) ADR-0005 authored once the branch is chosen. U3 is the gate.
3. **RC-10 = FLIP PRIVATE.** Operator action with a due date on the OPERATOR_ACTIONS.md register (CS-D); one `gh repo edit --visibility private` + confirm GHCR/CI unaffected.
4. **Plan routing = FRESH PLAN, ARCHIVE OLD.** Move the completed A132 IMPLEMENTATION_PLAN.md to docs/archived/ (existing precedent), generate fresh IMPLEMENTATION_PLAN.md from this analysis.
