# Operator Actions — Dated Register

> **This is the forcing-function register for post-merge operator actions** (RC-19, arch-review v5).
> It is the authoritative list for *operator/ops* actions that code alone cannot complete — live-host
> deploys, repo-settings changes, secret provisioning, external-account verifications, owner decisions.
> **GitHub issues remain authoritative for feature work**; this file tracks the manual steps that keep
> lapsing because nothing reminds anyone about them.
>
> **How it stays alive:** the monthly `secret-rotation` skill parses this file and Pushover-alerts on
> overdue/approaching items; the `monthly-audit` GitHub workflow renders it into its run summary + Slack
> post. Both fire on the 1st of each month. Keep the table format stable (the skill parses the rows).
>
> **Row format:** `| ID | Action | Due | Owner | Source | Status |`
> **Status values:** `OPEN` · `IN PROGRESS` · `DONE YYYY-MM-DD` · `BLOCKED (reason)`
> **Last reconciled:** 2026-07-14

## Open Actions

| ID | Action | Due | Owner | Source | Status |
|----|--------|-----|-------|--------|--------|
| OA-1 | Deploy migration 0036 to the homeserver (throwaway pgvector container, per deploy.md §4), recreate `workers` (`--no-deps`), then verify prod `retention_audit` for the 2026-07-05 & 2026-07-12 skills_log gaps and clear the ~2 stuck `data-retention-prune` BullMQ failed jobs | 2026-07-19 | Troy | Plan 1.3 / DA-1 / A135 | DONE 2026-07-14 (Entry 189: 0036 applied+verified, FK→SET NULL, rolled-back DELETE proved 6017 rows now prunable; workers recreated on new image healthy, scheduler clean. RESIDUAL: clear the 2 stuck failed-set records via Queues-tab "Clear failed" — redis-cli AUTH quirk deferred, non-blocking) |
| OA-2 | Flip repository visibility to **private** (`gh repo edit davistroy/open-brain --visibility private`); confirm GHCR images + CI still resolve afterward | 2026-07-19 | Troy | RC-10 (arch-review v5) | DONE 2026-07-14 (Entry 190: repo=PRIVATE; fresh homeserver `docker pull core-api:latest` still succeeds → GHCR package visibility independent of repo; CI uses GITHUB_TOKEN). **REVERSED 2026-07-14 (Entry 195): repo flipped BACK to PUBLIC** — private (free tier) disabled branch protection and broke `gh pr checks` + homeserver `git fetch` (Entry 194). Troy's decision: accept a public repo (no in-repo secrets) over losing protection. RC-10's "public repo" concern is now a DELIBERATE risk-acceptance. All 3 regressions verified fixed. |
| OA-3 | Close GitHub issue #226 using the prepared evidence in `docs/pending-issue-closures.md` (`gh issue close 226 --comment ...`) — fixed by PR #230 / 1710c54 (attribution nuance: issue said core-api, fix in workers daily-connections) | 2026-07-19 | Troy | Plan 2.4 / #226 | DONE 2026-07-13 (closed with prepared evidence, Entry 188) |
| OA-4 | **⚠ ELEVATED — this is an ACTIVE outage, not just the secret-rotation prereq (Entry 190, 2026-07-14).** `BWS_ACCESS_TOKEN` is absent from `/mnt/user/appdata/open-brain/.env.secrets` (0 lines) → BOTH `utility-ingest` AND `financial-ingest` sidecars fail their `bws secret list` calls (utility cron alert fired 3+ days). It's the Bitwarden **bootstrap** token (by design NOT in BWS; dropped by any `load-secrets.sh` reconcile). **Checklist:** (1) Get the open-brain machine-account **access token** from Bitwarden vault → Secrets Manager → Machine accounts → Access tokens (create a new one if lost/expired). (2) Append `BWS_ACCESS_TOKEN=<value>` to `.env.secrets` (root, keep mode 0600) — do NOT rely on `load-secrets.sh` (it won't write the bootstrap token). (3) `sudo docker compose up -d --force-recreate --no-deps utility-ingest financial-ingest workers`. (4) Verify `sudo docker exec open-brain-utility-ingest sh -c 'bws secret list >/dev/null && echo OK'`. Claude can do (2)–(4) if you place the token or provide it securely. **Also enables the monthly secret-rotation staleness check.** Durable fix offered: patch `load-secrets.sh` to PRESERVE an existing `BWS_ACCESS_TOKEN` on rewrite | 2026-07-16 | Troy | Plan 3.1 / RC-13 / Entry 190 | DONE 2026-07-14 (token placed via stdin; `.env.secrets` backed up; utility/financial/workers recreated; `bws` authenticates in all 3 — Entry 191/192). **RESIDUAL FOLLOW-UPS:** (a) ~~mint a dedicated **open-brain-scoped** BW machine token vs. the broad VM token used~~ → **WON'T-DO 2026-07-14 (D137/Entry 196):** Troy's call — keep the broad VM token; least-privilege gain not worth the extra provisioning/rotation surface for a solo system, and OA-4b already removed the recurring clobber. (b) ~~patch `load-secrets.sh` to PRESERVE `BWS_ACCESS_TOKEN` on rewrite~~ → **DONE 2026-07-14 (OA-4b, Entry 193/#268):** preservation + roundtrip test case 6.6 (7/7). (c) `financial-pipeline.py` naming drift: Pushover key-name fix merged Entry 193/#268 — but **it was NOT actually LIVE until 2026-07-15 (Entry 201)**: `financial-ingest` shares `ingest-sidecar:latest` with `utility-ingest` and was never named in a `--force-recreate`, so it sat on a **2026-05-09** image (still calling the old `get_bws_secret("pushover-user-key")`, which `sys.exit(1)`s). Resynced to `70fa72e6` — fix now live but **still unexercised** (A140);  **Plaid path DROPPED 2026-07-14 (D138/Entry 196)** — Troy is re-sourcing financial data a different way (TBD), so "provision 3 Plaid secrets" is CANCELLED and `init_plaid()`/`PLAID_*` config are dead-pending-replacement. |
| OA-5 | Verify the first *scheduled* offsite-backup (Fri 03:45) and restore-rehearsal (Sun 05:30) runs in homeserver logs / Pushover history; record the result in LAB_NOTEBOOK. Open since 2026-06-11 (~1 month unverified) | 2026-07-19 | Troy | A131 / PLT-H4 / RC-12 | DONE 2026-07-13 (both VERIFIED PASS via job logs — offsite-backup clean daily thru 07-13 03:45, rclone crypt 291 obj/4.93 GiB; restore-rehearsal 07-12 & 07-05 both `REHEARSAL RESULT: PASS` 21/21 tables ±0%. Entry 188. Pushover-delivery gap → OA-16) |
| OA-6 | Voice Bearer phase-2 rollout: create BWS secret `dev/open-brain/voice-capture-secret` (`openssl rand -hex 32`), add the `Authorization: Bearer` header to the iOS Shortcut AND `EXPO_PUBLIC_VOICE_SECRET` to the mobile build **first**, then set `VOICE_CAPTURE_SECRET` in `.env.secrets` and restart voice-capture + core-api. **Clients before server** or all direct captures 401 (runbook: voice-capture-auth.md) | 2026-08-02 | Troy | D132 / SEC-A3 / IA-M2 | OPEN |
| OA-7 | **U3 blocker for Plan 8.2** — verify whether the native mobile app currently passes CF Access on brain.troy-davis.com WITHOUT a CF Access service token. If it works without one → mobile has no auth control today → choose SEC-A2 Option 1 (dedicated api hostname). If CF Access is enforced → Option 2 (delete dead mobile-auth path) is acceptable. Record the finding. **[2026-07-13 code-side finding, Entry 188]** `packages/mobile` sends NO CF Access credential (grep for CF-Access/service-token = 0 hits) — auth is purely `Authorization: Bearer <ob_api_token>` + mobile-auth middleware + `mobile` rate tier. So mobile CANNOT traverse a CF-Access-gated hostname → favors **Option 1** (dedicated api.* hostname; mobile needs only an `EXPO_PUBLIC_API_URL` change, no code). Option 2 would break the LIVE app (mobile-auth is used, not dead). **REMAINING owner step:** confirm whether brain.troy-davis.com actually enforces CF Access on `/api/*` (vs. dashboard UI only) — device or CF dashboard. | 2026-08-09 | Troy | SEC-A2 / U3 (Plan 8.2) | OPEN |
| OA-8 | Promote `Validate init-schema.sql` + `Python lint & typecheck` to required branch-protection checks, keeping `enforce_admins=false`/`strict=false`. Confirm 2 consecutive green runs of each first | 2026-08-02 | Troy | Plan 6.6 / QA-4 | **BLOCKED 2026-07-14 — branch protection is DISABLED (Entry 194).** Flipping the repo private (OA-2) silently turned OFF *all* branch protection: GitHub free tier does NOT support protected branches on private repos (`GET/PUT .../branches/main/protection` → 403 "Upgrade to GitHub Pro or make this repository public"). So `main` is now UNPROTECTED — the prior required checks (`Integration tests`, `build-and-test`) are no longer enforced either. The 2 candidate checks ARE green on the last 3 runs (ready IF protection returns). **DECISION for Troy (this changes the OA-2 tradeoff):** (a) accept no branch protection [solo repo, lower risk]; or (b) upgrade to GitHub Pro/Team (~$4/mo) to get protection on private; or (c) revert to public (`gh repo edit --visibility public` — no secrets in-repo; protection + `gh pr checks` + homeserver `git fetch` all work again). Until one is chosen, OA-8 cannot proceed. → **RESOLVED + DONE 2026-07-14 (Entry 195):** Troy chose (c) revert to public. Branch protection was gone (404), so re-created via `gh api PUT .../protection` with ALL FOUR required checks — the original `Integration tests` + `build-and-test` PLUS OA-8's `Validate init-schema.sql` + `Python lint & typecheck` — `strict=false`, `enforce_admins=false`, no reviews. **OA-8 complete.** |
| OA-9 | Run the single live-host verification session: (a) confirm A131 backup/rehearsal runs, (b) test `WorkersMetricsAbsent` alert delivery by briefly stopping workers (PLT-H2), (c) deploy the batched compose window (workers backup-mount + BWS token) through the two-gate config-diff procedure | 2026-08-09 | Troy | Plan 7.5 / PLT-H2 / A131 | **PARTIAL 2026-07-14 (Entry 197):** the big deferred piece — the **non-root app-image transition** (core-api/slack-bot/voice-capture/web-next) + **OA-15 volume chown** — is DEPLOYED via the full-fleet "prod on latest" deploy (config-diff gate applied, postgres/redis stayed binds, all 6 healthy). (a) backups already verified in OA-5. **REMAINING:** (b) `WorkersMetricsAbsent` alert-delivery test (briefly stop workers), and (c) the workers `/backup-latest` mount — a COMPOSE change not adopted here (this deploy used the running compose + image-pull only; workers was already current and not recreated). Also still deferred: **OA-10** postgres `shm_size` (needs a postgres recreate). |
| OA-10 | Postgres `shm_size` — **SUPERSEDED 2026-07-15: now PR #297, owned by the homeserver upgrade audit's CS-4** (`~/dev/personal/homeserver/IMPLEMENTATION_PLAN_UPGRADES.md`). **PROMOTED from "optional" to a latent DR bug**: `max_parallel_maintenance_workers=2` is `source=default` (verified live), `pg_restore` builds HNSW indexes *after* loading ⇒ **the current dump probably cannot be restored** — it fails exactly when you are already in an incident. Value is `1gb`, not `512mb`. **D147: it lands FIRST and its recreate goes ALONE** (D134 isolated-waves). 🚨 **The recreate MUST pass `-f docker-compose.override.yml`** or the DB comes up EMPTY. ⚠️ **#297 ships half its own atomic pair** — no `max_parallel_maintenance_workers` pin (flagged upstream, commit `19cf411`). | with PR #297 | Troy | DA-11 / homeserver CS-4 / #297 | OPEN (upstream) |
| OA-11 | Quarterly vendor-terms verification: confirm OpenAI "no training on API data" and Deepgram "0-day retention" under the CURRENT plan/terms (see docs/PROVIDER_SETTINGS.md placeholders). **[2026-07-13 public-terms research, Entry 188]** OpenAI = no-training-on-API-data by default (policy since 2023-03-01) + 30-day abuse-log retention, ZDR available on approval. **Deepgram CORRECTION:** self-service hosted API is AUTO-ENROLLED in the Model Improvement Program (audio persisted + used for training) UNLESS `mip_opt_out=true` — the PROVIDER_SETTINGS Deepgram "0-day" note understates this; reconcile it here. Mitigant: prod transcription is self-hosted faster-whisper, so Deepgram is latent/optional (voice-pipecat), not a live egress — confirm which provider is wired before treating it as active. **REMAINING (owner login):** OpenAI org data-controls (ZDR/opt-in state) + Deepgram account MIP/DPA status. | 2026-09-30 | Troy | RI-3 / RC | OPEN |
| OA-12 | **Gmail OAuth: PUBLISH the app out of "Testing" mode** (Google Cloud Console). **RE-SCOPED 2026-07-15 (Entry 209): this is NOT a "re-consent".** Re-consenting restores service for exactly **7 days** and re-arms the trap — Testing-mode refresh tokens expire on a 7-day clock, which is why the token died 2026-04-21, ~7 days after the last hand-deploy. **Required EVEN IF obvm is decommissioned (D144)**: the TS `gmail-client` was seeded from the **same OAuth client**, so it dies on the same clock. Console access = Troy's account, no API. (Python-side interactive re-consent additionally needs an SSH tunnel — `run_local_server(port=0)` binds an ephemeral localhost port on a **headless** VM — but that half is moot under D144.) | 2026-07-26 | Troy | operational-followups / RC-13 / #282 | **DONE 2026-07-16** — Troy PUBLISHED the app to production (Google Auth Platform → Audience), confirmed "In production"; then re-consented via a no-Tailscale manual loopback code-copy on obvm (Entry 226). Verified live (getProfile OK + pipeline's own `authenticate()` returns True). #282 closed. |
| OA-13 | Merge Dependabot PRs #235/#237/#238 (cloudflare/email-worker: postal-mime, @cloudflare/workers-types 4→5) now that `email-worker-test` CI job (npm ci + tsc --noEmit + vitest, verified green locally 16/16) gates the package. Confirm the new job is green on each PR before merging | 2026-07-19 | Troy | Plan 6.5 / QA-7 / SW5-L13 | DONE 2026-07-14 (Entry 195): `@dependabot rebase`'d #235/#237 onto the fixed CI → `email-worker-test` job GREEN validated both bumps → merged. (#238 no longer exists.) |
| OA-14 | Merge the 5 GitHub-Actions major Dependabot PRs (#242 docker/build-push-action 6→7, #241 actions/cache 5→6, #240 setup-python 5→6, #239 pnpm/action-setup 4→6, #236 setup-node 5→6) **one at a time**, watching one post-merge `build-images.yml` run after each. The new `notify-failure` job (Plan 7.1) now Slack-alerts a broken build so a bad major merge can't silently stale `:latest`. #242 touches all 8 build steps — merge it last/most carefully | 2026-07-26 | Troy | Plan 7.1 / PE-M9 | **DONE 2026-07-14 (Entry 196).** All 5 merged one-at-a-time in order #240→#241→#236→#239→#242, each rebased-against-current-main first (via `gh api PUT .../update-branch` — Dependabot's rebase queue was flaky) and each post-merge `build-images` run confirmed green (notify-failure skipped every time). Research (5-agent workflow) pre-verified every breaking-change surface: setup-node v6 keeps `package-manager-cache: false` (doc-sync/email-worker green ✓); pnpm v6 honors `version: 9.15.0` — log showed `Switching pnpm from v11.7.0 to v9.15.0` ✓; build-push v7 = all 8 images pushed `:latest` + gha cache round-tripped ✓. main @ `677e5a71`. **No homeserver deploy needed** — CI-tooling bumps only; app-image contents unchanged. |

| OA-15 | When deploying the Phase-8 non-root container images (core-api, slack-bot, voice-capture, web-next now run `USER node`/1000): the pre-existing **root-owned named volumes** must be chowned once or writes fail — `docker run --rm -v open-brain_admin_prewipe_backup:/v alpine chown -R 1000:1000 /v` (admin pre-wipe pg_dump) and `docker run --rm -v open-brain_voice_spool_data:/v alpine chown -R 1000:1000 /v` (voice dead-letter spool). Do this in the OA-9 compose window before recreating those services | 2026-08-09 | Troy | Plan 8.5 / SEC-A6 | **DONE 2026-07-14 (Entry 197):** both volumes chowned to `1000:1000` (verified `stat`) as part of the full-fleet deploy; the non-root core-api + voice-capture recreated `[healthy]` and write their now-1000-owned volumes. Non-root images (core-api/slack-bot/voice-capture/web-next) are LIVE. |
| OA-16 | Restore-rehearsal success Pushover POST is FAILING — both the 2026-07-12 and 2026-07-05 runs end with `WARN: Pushover POST failed (curl exit 22)` (HTTP ≥ 400) immediately AFTER `REHEARSAL RESULT: PASS`. The DR rehearsal itself works; the owner just never receives the confirmation, so a future *failed* rehearsal could also go unseen. Verify `PUSHOVER_APP_TOKEN`/`PUSHOVER_USER_KEY` in homeserver `.env.secrets` and send a manual test POST to api.pushover.net; also confirm Pushover delivery history for `backup.sh` success alerts (couldn't be checked read-only). Surfaced during OA-5 verification. **[2026-07-15 LEAD, Entry 202/#278]** possibly related: `secrets-map.sh`'s BWS names are wrong for 11 of 14 required secrets (it wants `open-brain-pushover-app-token`; BWS actually has `PUSHOVER_API_TOKEN`). Prod `.env.secrets` DOES carry `PUSHOVER_APP_TOKEN`/`PUSHOVER_USER_KEY` (verified in the workers/core-api env), so the vars exist — the rehearsal's `curl exit 22` is more likely a stale/invalid VALUE than a missing var. Check #278 before debugging this in isolation. | 2026-07-26 | Troy | Entry 188 / OA-5 spinoff | **ROOT-CAUSED + FIXED 2026-07-16 (Entry 227, PR #337).** NOT a credential problem — the token is valid (manual POST returns 200). `scripts/lib/pushover-notify.sh` used `curl -d "message="` (no URL-encoding); the literal `%` in the message `…±0% tolerance…` is an invalid x-www-form-urlencoded escape → Pushover 400 → every run failed since 2026-06. Fix = `--data-urlencode` for the text fields + CI regression test. **Host `pushover-notify.sh` HOT-PATCHED + live-verified 2026-07-16** (delivers now), so the 2026-07-19 rehearsal will notify. ⚠️ **Merge PR #337 to make durable** — a full `git checkout origin/main` on the host would otherwise revert the hot-patch to the buggy version. |
| OA-17 | **Private-repo regression (from OA-2 flip):** the homeserver git repo `/mnt/user/appdata/open-brain` has an unauthenticated HTTPS `origin` → `git fetch`/`git checkout origin/main -- <file>` now fails (`could not read Username for 'https://github.com'`). **This BLOCKS the documented compose/migration deploy pattern** (git-checkout a single file from origin/main — Entry 179). Fix: either `git remote set-url origin git@github.com:davistroy/open-brain.git` + add a repo **deploy key** to the homeserver's SSH (github.com), OR configure an HTTPS credential helper with a fine-grained PAT. Verify with `git -C /mnt/user/appdata/open-brain fetch origin`. **Do before the next compose/migration deploy.** (Minor sibling: the VM's `gh` fine-grained PAT lost `gh pr checks` on the now-private repo — grant it `checks:read`+`statuses:read`; workaround = Actions API.) | 2026-07-21 | Troy | Entry 192 / OA-2 regression | OBSOLETE 2026-07-14 (Entry 195): resolved by reverting to public — homeserver anonymous `git fetch origin` VERIFIED working; `gh pr checks` VERIFIED working. No deploy key / PAT scope needed while public. |
| OA-18 | **Install the `actual-ingest` host cron** (`0 6 * * *` = 6am ET natively — the homeserver's TZ is `America/New_York`). Root only: append to `/boot/config/plugins/dynamix/custom.cron`, then `/usr/local/sbin/update_cron`. 🚨 **Editing `deploy/cron/unraid-ingest.cron` schedules NOTHING** — proven 2026-07-15: `0 7 --balances` and `5 7 --account-monitoring` sit in the repo template but are **NOT installed on the host**. `claude` cannot write `/boot/config`. | with the actual-ingest deploy | Troy | spec 2026-07-15 / Entry 208 | **DONE 2026-07-16** (Entry 230) — actual-daily `0 6` ET installed in `custom.cron` + `/etc/cron.d/root`; the exact `docker exec … node /app/actual-daily.mjs` was validated by a manual run (capture posted, exit 0). |
| OA-19 | **Create `config/payee-rules.yaml` on the homeserver** (gitignored, host-only by design — one copy cannot drift, the `.env.secrets` model). Sited at `config/` **top level** so it rides `backup.sh`'s existing glob; the tracked `config/payee-rules.example.yaml` carries the schema. **Contains merchant names = financial data — never commit it.** | with the actual-ingest deploy | Troy | spec 2026-07-15 / D145-adjacent | **DONE 2026-07-16** (Entry 228/230) — GENERATED host-only from Troy's real Actual categorizations (read-only netns dump): 15 categories, 142 payees, 14 transfer excludes, 0 investment (heuristic). Refine from the first run's 8 unmatched. |
| OA-20 | **Add 3 BWS secrets for actual-ingest** (password, sync ID, server URL) + wire all 3 through the lockstep (`deploy/.env.secrets.template` + `scripts/lib/secrets-map.sh`). ⚠️ **Verify the real BWS names first** — #278 found 11 of 14 mapped names were invented and matched nothing. | with the actual-ingest deploy | Troy | spec §7 / Entry 208 | **DONE 2026-07-16** (Entry 228) — 3 BWS secrets created in `ai-work` under the loader keys (password copied from the existing `Actual Budget — My Finances` secret). ⚠️ Hydrated into `.env.secrets` by SURGICAL APPEND, not full `load-secrets.sh` — a live map-vs-BWS check found **9 mapped keys with wrong BWS names** (SMTP×4/slack-user/grafana + `dev/` vs `open-brain-` prefix drift on mobile/voice/trigger). **That map drift is a separate live DR gap → NEW follow-up needed (a rebuilt host's `load-secrets.sh` silently drops those 9).** |
| OA-21 | **Confirm the router's DHCP pool floor sits ABOVE the `actual-ingest` static br0 IP.** `br0` declares the whole `/24` with **no `ip-range`**, so Docker auto-IPAM could collide with a real LAN client; every br0 container is hand-assigned. The address was verified **unclaimed** (no ping, ARP `INCOMPLETE`) — **"unclaimed today" is not "reserved"**. Router admin only. | before the actual-ingest deploy | Troy | spec §3.1 / D142 | **DONE 2026-07-16** — Troy confirmed the DHCP pool floor is **.101**, well above the `.13` static. No collision. |
| OA-22 | **Decide: is power ingestion (Cobb EMC) still wanted?** #286 is **an unbuilt feature, not a bug** — nothing invokes the binary, the CSV parser is unwritten (`cmd_power_summary:670-671` says so in a TODO), and `data_dir` isn't a volume so CSVs vanish on recreate. Fixing the Dockerfile buys a working binary that still produces **zero rows**. | 2026-08-15 | Troy | #286 / Entry 209 | **DECIDED 2026-07-16: BUILD, deferred.** Troy wants power tracking; scoped build task captured on issue #286 (probe `electric-usage-downloader` for Cobb EMC support FIRST, then CSV parser + data volume + cron). #286 stays OPEN as the ready-to-build task. |
| OA-23 | **Decide: pursue Cobb Water B2C OIDC?** Approach A (headless browser — a new runtime dep in the shared sidecar image) vs B (replicate OIDC — *"MFA handling is very difficult without a browser"*, per this repo's own doc). 🚨 **A bogus-credential probe risks ACCOUNT LOCKOUT** — Entry 198's Gas South trick does **not** port safely to B2C. The API was **never anonymous**; the "confirmed via HAR analysis" comment predates the analysis by 8h. | 2026-08-15 | Troy | #285 / Entry 209 | **DONE 2026-07-16: CLOSED won't-do.** Azure B2C + no downloader tool + account-lockout risk >> monthly-readings value. #285 closed as not-planned. |
| OA-24 | **Disable obvm's email-pipeline cron (G-B.2) and close G-B.5** — the VM-decommission decision, **open since 2026-04-17**. Resolved by **D144: decommission**. **Gate: prove `email-classify` parity FIRST** (plan Phase 3; start with the `email_classifications`-by-hour query, which needs no host access). Then remove `email-pipeline` from `BYPASS_CALLERS` and move G-B.2/G-B.5 out of `docs/archived/` — live unexecuted decisions must not live where nobody looks. Needs obvm ssh + crontab. | 2026-08-15 | Troy | #290 / D144 / Entry 209 | **GATE NEARLY CLEARED 2026-07-16 (Entry 231).** Parity query done: in-stack `email-classify` covers hotmail (7101 rows, resumes post-scheduler-fix) + gmail (**re-consented 2026-07-16** — was dead since 04-21; durable now that A1 published the app; live-verified Google mints an access_token). obvm's pipeline is now redundant. **Final confirm: the 07-17 05:00 run should produce BOTH provider rows** — then disable obvm's email cron, drop `email-pipeline` from BYPASS_CALLERS, decommission the VM. Still OPEN (obvm not yet retired). |
| OA-27 | **Install the compose parity-drift cron** (`deploy/cron/unraid-parity-check.cron`, daily 05:15) so between-deploy compose drift becomes a Pushover instead of staying silent (#302). Root only: append to `/boot/config/plugins/dynamix/custom.cron` + `/usr/local/sbin/update_cron`. Runs on the homeserver reading the deployed compose locally (no self-SSH); the line sources `.env.secrets` for Pushover creds. Committing the template installs nothing (same as the ingest crons). | 2026-08-15 | Troy | #302 / D148 | **DONE 2026-07-16** (Entry 230) — parity cron `15 5` ET installed in `custom.cron` + `/etc/cron.d/root`; test-run exit 0 ("parity OK — no alert"). |
| OA-26 | **Create the BWS item `dev/open-brain/ingest-trigger-secret`** so `INGEST_TRIGGER_SECRET` survives a DR rebuild (#300). Generate: `openssl rand -hex 32`. Value must match what's in the running prod `.env.secrets` today (or rotate both together — it's the bearer token core-api/workers present to the sidecar `/process` endpoint). Until this exists, `load-secrets.sh` writes `.env.secrets` WITHOUT it on a rebuild → both sidecars 401 every HTTP trigger while host cron looks healthy. Repo side (template + map, OPTIONAL) is done. | 2026-08-15 | Troy | #300 / A144 | **DONE 2026-07-17 (#340, Entry 233) — NOT a creation.** The BWS item ALREADY EXISTS as `open-brain-ingest-trigger-secret`; secrets-map.sh pointed at the wrong `dev/open-brain/ingest-trigger-secret` and silently dropped it on DR. Fixed by the #340 map rename + a recorded-key-inventory CI guard (test 6.7). No BWS mutation, no rotation needed. |
| OA-25 | **Rename the Pushover application (or split infra to its own app)** so alerts stop reading **"From Voice Capture"** — the human-factors root cause the 40h scheduler outage went unactioned (Entries 211/214). The banner is set in the pushover.net console, not per message. **Option A (recommended, 1 step):** rename the "Voice Capture" app → "Open Brain" (token unchanged → zero code/env/Alertmanager change; also re-brands voice-memo notifications). **Option B:** create an "Open Brain Infra" app and point the **observability** project's Alertmanager `pushover-critical`/`-warning` receivers at its token — that is the path that pages during outages. The code-side title prefix (#312, shipped) already makes in-app titles read "Open Brain: …". Full steps: `docs/runbooks/pushover-alert-branding.md`. pushover.net (+ observability Alertmanager for B) = Troy's accounts only. | 2026-08-02 | Troy | #312 / Entry 214 | OPEN |
| OA-28 | **Deploy #298 (voice_sessions removal)** — repo done (PR #342 on main). Recreate core-api/workers/web-next on the new images FIRST (they no longer reference voice_sessions), THEN apply migration `0037_drop_voice_sessions.sql` via `migrate-manual.sh` — a DESTRUCTIVE `DROP TABLE CASCADE` (drops the frozen rows). **Order matters:** dropping before the new workers code deploys makes the OLD retention job prune a missing table. Not urgent (feature is dead). Compose unchanged → app-only recreate, not a #302 window. | 2026-08-15 | Troy | #298 / Entry 232 | OPEN |
| OA-29 | **Confirm the 07-17 05:00 email-classify run produced BOTH gmail + hotmail rows** (`select provider,count(*) from email_classifications where processed_at > now()-interval '1 day' group by provider`), then RETIRE obvm (folds OA-24): disable obvm's email-pipeline cron, drop `email-pipeline` from `BYPASS_CALLERS`, decommission the VM. Closes #290/#284/D144. | 2026-07-24 | Troy/Claude | #290 / Entry 231 | **PARITY CONFIRMED 2026-07-17** (Entry 235): gmail=4, hotmail=5 from the 05:00 run — repo-side bypass removed (PR #347). REMAINING: disable obvm cron + decommission VM + close #290/#284. Still OPEN. |
| OA-30 | **Scrub the tracked utility-config leak (financial account IDs in a PUBLIC repo).** `config/utility/utility-config.yaml` is git-TRACKED (its own `.example` wrongly says it's gitignored) and `scripts/utility-pipeline.py` HEAD hardcoded 3 account IDs (removed on the branch, but they persist in HISTORY). Full scrub: (1) **on the homeserver, back up** `config/utility/utility-config.yaml` (host-only real config) before anything; (2) `git rm --cached config/utility/utility-config.yaml` + add it to `.gitignore` (mirror the `payee-rules.yaml` precedent) + commit; (3) `git filter-repo` (or BFG) to purge the file AND the historical `utility-pipeline.py` account-ID strings from all history; (4) **force-push** `main`; (5) **reset every clone** (homeserver `/mnt/user/appdata/open-brain`, the VM) — the homeserver clone will otherwise DELETE the host-only config on pull, so restore it from the backup afterward. Treat the IDs as already-public (they've been on GitHub). Root only (force-push + host access). | 2026-08-15 | Troy | #286 / Entry 236 | OPEN |
| OA-31 | **Deploy #286 Cobb EMC power** once the code (PR — power parser + `cmd_power_summary`) merges. (1) **Probe (5.1):** confirm 2FA is OFF on the Cobb EMC SmartHub account, then run the pinned `electric-usage-downloader` v2.4.1 against `cobbemc.smarthub.coop` → confirm it writes interval CSV (columns `StartUnixMillis,…`). If it only writes InfluxDB, use a manual Green Button CSV export into `data_dir` (same parser). (2) On the host `utility-config.yaml`, set `power.data_dir: /data/electric-usage` + `power.timezone` + `power.bitwarden_username_key/password_key` (COBB_EMC_USERNAME/PASSWORD); ensure the two BWS secrets exist. (3) Template `electric-usage-downloader-config.yaml` creds + `account`/`service_location` from BWS/SmartHub at run time (never commit real values). (4) Install the host cron that RUNS the downloader before `--power-summary` (`/boot/config/plugins/dynamix/custom.cron` + `update_cron` — editing `deploy/cron/unraid-ingest.cron` installs nothing). (5) **Rebuild BOTH** `utility-ingest` + `financial-ingest` (shared image) and verify `power_readings` populates + `--monthly-comparison` includes power. | 2026-08-15 | Troy | #286 / Entry 236 | OPEN |
| OA-32 | **Finish #281 on the RUNNING stateful trio (postgres/redis/cloudflared → D145 `env_file`).** The 2026-07-17 deploy (Entry 239) applied D145 to the APP services but deliberately did NOT recreate the running postgres/redis/cloudflared (they run their pre-D145 config; DR-safety is already achieved by the code on `main`). So the host compose (D145, `redis:7.4-alpine`, `sh -c` command, `env_file`) now DRIFTS from the running `redis:7-alpine` etc. — the parity cron will flag it. In a deliberate stateful window: verify binds render (config-diff gate), then `up -d --force-recreate --no-deps postgres redis cloudflared`. ⚠️ `.env.secrets` already carries the synthesized `POSTGRES_URL`/`REDIS_URL`/`TUNNEL_TOKEN`/`REDIS_PASSWORD` (manually added since `bws` isn't on the host — see OA-33). Low urgency. **ALSO (host-only, DONE 2026-07-17):** removed the redundant `core-api: ports: ["3002:3000"]` (=`0.0.0.0:3002`) from `docker-compose.override.yml` — it overlapped the base's specific-IP binds (broke recreate) AND re-opened core-api to the LAN. Keep it removed. | 2026-08-30 | Troy | #281 / Entry 239 | OPEN |
| OA-33 | **`bws` is NOT installed on the Unraid host** (only inside the sidecar containers). So `scripts/load-secrets.sh` cannot run on the homeserver — the OA-4 / load-secrets runbook ("run it on the homeserver after a rebuild") is WRONG for a from-scratch DR unless `bws` is installed first. For the 2026-07-17 deploy I synthesized the D145 `.env.secrets` keys by hand from the host `.env`. Fix: install the `bws` CLI on the Unraid host (nerdpack / static binary to `/usr/local/bin`) OR document that `.env.secrets` regen must run from a machine/container that HAS `bws` (e.g. `docker exec` a sidecar, or the VM). This is a REAL DR gap — a rebuilt host can't regenerate `.env.secrets` locally today. | 2026-08-30 | Troy | Entry 239 / #281 | OPEN |
| OA-34 | ~~Merge + deploy the `python:3.14-slim` ingest bump.~~ **✅ DONE 2026-07-17 (Entries 241–242).** Verified in-image (PyMuPDF 1.28.0 cp310-abi3 covers 3.14; 42/42 sidecar + 26/26 file-ingestion tests on 3.14; `PyYAML==6.0.2`→`6.0.3` for a clean cp314 wheel). PR **#354** (supersedes/closed #321) merged; `build-images` rebuilt `ingest-sidecar:latest` + `file-ingestion:latest`; DEPLOYED to prod — pulled new images (sidecar `a34c53c`→`7c34dc2`, file-ingestion `fc5843a`→`498dce7`), `--force-recreate --no-deps utility-ingest financial-ingest file-ingestion`; all 3 now **python 3.14.6**, PyMuPDF+parsers load, file-ingestion `/health` healthy, **captures 11306 unchanged**, all 13 containers healthy. postgres/redis untouched (binds). | 2026-07-17 | Troy/Claude | #321 / Entry 242 | ✅ DONE |

## Completed Actions

| ID | Action | Completed | Source |
|----|--------|-----------|--------|
| — | (none yet — items move here with `DONE YYYY-MM-DD` when finished) | — | — |
