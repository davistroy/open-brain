# Operator Actions — Dated Register

> **This is the forcing-function register for post-merge operator actions** (RC-19, arch-review v5).
> It is the authoritative list for *operator/ops* actions that code alone cannot complete — live-host
> deploys, repo-settings changes, secret provisioning, external-account verifications, owner decisions.
> **GitHub issues remain authoritative for feature work**; this file tracks the manual steps that keep
> lapsing because nothing reminds anyone about them.
>
> **How it stays alive:** the monthly `secret-rotation` skill parses this file and Pushover-alerts on
> overdue/approaching items; the `monthly-audit` GitHub workflow renders it into its run summary + Slack
> post. Both fire on the 1st of each month. Keep the table format stable (the skill parses the rows).
>
> **Row format:** `| ID | Action | Due | Owner | Source | Status |`
> **Status values:** `OPEN` · `IN PROGRESS` · `DONE YYYY-MM-DD` · `BLOCKED (reason)`
> **Last reconciled:** 2026-07-14

## Open Actions

| ID | Action | Due | Owner | Source | Status |
|----|--------|-----|-------|--------|--------|
| OA-1 | Deploy migration 0036 to the homeserver (throwaway pgvector container, per deploy.md §4), recreate `workers` (`--no-deps`), then verify prod `retention_audit` for the 2026-07-05 & 2026-07-12 skills_log gaps and clear the ~2 stuck `data-retention-prune` BullMQ failed jobs | 2026-07-19 | Troy | Plan 1.3 / DA-1 / A135 | DONE 2026-07-14 (Entry 189: 0036 applied+verified, FK→SET NULL, rolled-back DELETE proved 6017 rows now prunable; workers recreated on new image healthy, scheduler clean. RESIDUAL: clear the 2 stuck failed-set records via Queues-tab "Clear failed" — redis-cli AUTH quirk deferred, non-blocking) |
| OA-2 | Flip repository visibility to **private** (`gh repo edit davistroy/open-brain --visibility private`); confirm GHCR images + CI still resolve afterward | 2026-07-19 | Troy | RC-10 (arch-review v5) | DONE 2026-07-14 (Entry 190: repo=PRIVATE; fresh homeserver `docker pull core-api:latest` still succeeds → GHCR package visibility independent of repo; CI uses GITHUB_TOKEN). **REVERSED 2026-07-14 (Entry 195): repo flipped BACK to PUBLIC** — private (free tier) disabled branch protection and broke `gh pr checks` + homeserver `git fetch` (Entry 194). Troy's decision: accept a public repo (no in-repo secrets) over losing protection. RC-10's "public repo" concern is now a DELIBERATE risk-acceptance. All 3 regressions verified fixed. |
| OA-3 | Close GitHub issue #226 using the prepared evidence in `docs/pending-issue-closures.md` (`gh issue close 226 --comment ...`) — fixed by PR #230 / 1710c54 (attribution nuance: issue said core-api, fix in workers daily-connections) | 2026-07-19 | Troy | Plan 2.4 / #226 | DONE 2026-07-13 (closed with prepared evidence, Entry 188) |
| OA-4 | **⚠ ELEVATED — this is an ACTIVE outage, not just the secret-rotation prereq (Entry 190, 2026-07-14).** `BWS_ACCESS_TOKEN` is absent from `/mnt/user/appdata/open-brain/.env.secrets` (0 lines) → BOTH `utility-ingest` AND `financial-ingest` sidecars fail their `bws secret list` calls (utility cron alert fired 3+ days). It's the Bitwarden **bootstrap** token (by design NOT in BWS; dropped by any `load-secrets.sh` reconcile). **Checklist:** (1) Get the open-brain machine-account **access token** from Bitwarden vault → Secrets Manager → Machine accounts → Access tokens (create a new one if lost/expired). (2) Append `BWS_ACCESS_TOKEN=<value>` to `.env.secrets` (root, keep mode 0600) — do NOT rely on `load-secrets.sh` (it won't write the bootstrap token). (3) `sudo docker compose up -d --force-recreate --no-deps utility-ingest financial-ingest workers`. (4) Verify `sudo docker exec open-brain-utility-ingest sh -c 'bws secret list >/dev/null && echo OK'`. Claude can do (2)–(4) if you place the token or provide it securely. **Also enables the monthly secret-rotation staleness check.** Durable fix offered: patch `load-secrets.sh` to PRESERVE an existing `BWS_ACCESS_TOKEN` on rewrite | 2026-07-16 | Troy | Plan 3.1 / RC-13 / Entry 190 | DONE 2026-07-14 (token placed via stdin; `.env.secrets` backed up; utility/financial/workers recreated; `bws` authenticates in all 3 — Entry 191/192). **RESIDUAL FOLLOW-UPS:** (a) mint a dedicated **open-brain-scoped** BW machine token vs. the broad VM token used [least-privilege]; (b) patch `load-secrets.sh` to PRESERVE `BWS_ACCESS_TOKEN` on rewrite [durable — the recurring clobber source]; (c) `financial-pipeline.py` may have the SAME `plaid-*`/`pushover-*` naming drift — verify before relying on financial ingest. |
| OA-5 | Verify the first *scheduled* offsite-backup (Fri 03:45) and restore-rehearsal (Sun 05:30) runs in homeserver logs / Pushover history; record the result in LAB_NOTEBOOK. Open since 2026-06-11 (~1 month unverified) | 2026-07-19 | Troy | A131 / PLT-H4 / RC-12 | DONE 2026-07-13 (both VERIFIED PASS via job logs — offsite-backup clean daily thru 07-13 03:45, rclone crypt 291 obj/4.93 GiB; restore-rehearsal 07-12 & 07-05 both `REHEARSAL RESULT: PASS` 21/21 tables ±0%. Entry 188. Pushover-delivery gap → OA-16) |
| OA-6 | Voice Bearer phase-2 rollout: create BWS secret `dev/open-brain/voice-capture-secret` (`openssl rand -hex 32`), add the `Authorization: Bearer` header to the iOS Shortcut AND `EXPO_PUBLIC_VOICE_SECRET` to the mobile build **first**, then set `VOICE_CAPTURE_SECRET` in `.env.secrets` and restart voice-capture + core-api. **Clients before server** or all direct captures 401 (runbook: voice-capture-auth.md) | 2026-08-02 | Troy | D132 / SEC-A3 / IA-M2 | OPEN |
| OA-7 | **U3 blocker for Plan 8.2** — verify whether the native mobile app currently passes CF Access on brain.troy-davis.com WITHOUT a CF Access service token. If it works without one → mobile has no auth control today → choose SEC-A2 Option 1 (dedicated api hostname). If CF Access is enforced → Option 2 (delete dead mobile-auth path) is acceptable. Record the finding. **[2026-07-13 code-side finding, Entry 188]** `packages/mobile` sends NO CF Access credential (grep for CF-Access/service-token = 0 hits) — auth is purely `Authorization: Bearer <ob_api_token>` + mobile-auth middleware + `mobile` rate tier. So mobile CANNOT traverse a CF-Access-gated hostname → favors **Option 1** (dedicated api.* hostname; mobile needs only an `EXPO_PUBLIC_API_URL` change, no code). Option 2 would break the LIVE app (mobile-auth is used, not dead). **REMAINING owner step:** confirm whether brain.troy-davis.com actually enforces CF Access on `/api/*` (vs. dashboard UI only) — device or CF dashboard. | 2026-08-09 | Troy | SEC-A2 / U3 (Plan 8.2) | OPEN |
| OA-8 | Promote `Validate init-schema.sql` + `Python lint & typecheck` to required branch-protection checks, keeping `enforce_admins=false`/`strict=false`. Confirm 2 consecutive green runs of each first | 2026-08-02 | Troy | Plan 6.6 / QA-4 | **BLOCKED 2026-07-14 — branch protection is DISABLED (Entry 194).** Flipping the repo private (OA-2) silently turned OFF *all* branch protection: GitHub free tier does NOT support protected branches on private repos (`GET/PUT .../branches/main/protection` → 403 "Upgrade to GitHub Pro or make this repository public"). So `main` is now UNPROTECTED — the prior required checks (`Integration tests`, `build-and-test`) are no longer enforced either. The 2 candidate checks ARE green on the last 3 runs (ready IF protection returns). **DECISION for Troy (this changes the OA-2 tradeoff):** (a) accept no branch protection [solo repo, lower risk]; or (b) upgrade to GitHub Pro/Team (~$4/mo) to get protection on private; or (c) revert to public (`gh repo edit --visibility public` — no secrets in-repo; protection + `gh pr checks` + homeserver `git fetch` all work again). Until one is chosen, OA-8 cannot proceed. → **RESOLVED + DONE 2026-07-14 (Entry 195):** Troy chose (c) revert to public. Branch protection was gone (404), so re-created via `gh api PUT .../protection` with ALL FOUR required checks — the original `Integration tests` + `build-and-test` PLUS OA-8's `Validate init-schema.sql` + `Python lint & typecheck` — `strict=false`, `enforce_admins=false`, no reviews. **OA-8 complete.** |
| OA-9 | Run the single live-host verification session: (a) confirm A131 backup/rehearsal runs, (b) test `WorkersMetricsAbsent` alert delivery by briefly stopping workers (PLT-H2), (c) deploy the batched compose window (workers backup-mount + BWS token) through the two-gate config-diff procedure | 2026-08-09 | Troy | Plan 7.5 / PLT-H2 / A131 | OPEN |
| OA-10 | Add `shm_size: "512mb"` to the postgres compose service during the next daemon-restart window (needs a postgres recreate — batch it; prevents the `/dev/shm` 64 MB wall on future parallel-index migrations) | next restart window | Troy | DA-11 / CLAUDE.md deferral | OPEN |
| OA-11 | Quarterly vendor-terms verification: confirm OpenAI "no training on API data" and Deepgram "0-day retention" under the CURRENT plan/terms (see docs/PROVIDER_SETTINGS.md placeholders). **[2026-07-13 public-terms research, Entry 188]** OpenAI = no-training-on-API-data by default (policy since 2023-03-01) + 30-day abuse-log retention, ZDR available on approval. **Deepgram CORRECTION:** self-service hosted API is AUTO-ENROLLED in the Model Improvement Program (audio persisted + used for training) UNLESS `mip_opt_out=true` — the PROVIDER_SETTINGS Deepgram "0-day" note understates this; reconcile it here. Mitigant: prod transcription is self-hosted faster-whisper, so Deepgram is latent/optional (voice-pipecat), not a live egress — confirm which provider is wired before treating it as active. **REMAINING (owner login):** OpenAI org data-controls (ZDR/opt-in state) + Deepgram account MIP/DPA status. | 2026-09-30 | Troy | RI-3 / RC | OPEN |
| OA-12 | Gmail OAuth re-consent (token refresh window) for the email-classify pipeline | 2026-07-26 | Troy | operational-followups / RC-13 | OPEN |
| OA-13 | Merge Dependabot PRs #235/#237/#238 (cloudflare/email-worker: postal-mime, @cloudflare/workers-types 4→5) now that `email-worker-test` CI job (npm ci + tsc --noEmit + vitest, verified green locally 16/16) gates the package. Confirm the new job is green on each PR before merging | 2026-07-19 | Troy | Plan 6.5 / QA-7 / SW5-L13 | DONE 2026-07-14 (Entry 195): `@dependabot rebase`'d #235/#237 onto the fixed CI → `email-worker-test` job GREEN validated both bumps → merged. (#238 no longer exists.) |
| OA-14 | Merge the 5 GitHub-Actions major Dependabot PRs (#242 docker/build-push-action 6→7, #241 actions/cache 5→6, #240 setup-python 5→6, #239 pnpm/action-setup 4→6, #236 setup-node 5→6) **one at a time**, watching one post-merge `build-images.yml` run after each. The new `notify-failure` job (Plan 7.1) now Slack-alerts a broken build so a bad major merge can't silently stale `:latest`. #242 touches all 8 build steps — merge it last/most carefully | 2026-07-26 | Troy | Plan 7.1 / PE-M9 | OPEN |

| OA-15 | When deploying the Phase-8 non-root container images (core-api, slack-bot, voice-capture, web-next now run `USER node`/1000): the pre-existing **root-owned named volumes** must be chowned once or writes fail — `docker run --rm -v open-brain_admin_prewipe_backup:/v alpine chown -R 1000:1000 /v` (admin pre-wipe pg_dump) and `docker run --rm -v open-brain_voice_spool_data:/v alpine chown -R 1000:1000 /v` (voice dead-letter spool). Do this in the OA-9 compose window before recreating those services | 2026-08-09 | Troy | Plan 8.5 / SEC-A6 | OPEN |
| OA-16 | Restore-rehearsal success Pushover POST is FAILING — both the 2026-07-12 and 2026-07-05 runs end with `WARN: Pushover POST failed (curl exit 22)` (HTTP ≥ 400) immediately AFTER `REHEARSAL RESULT: PASS`. The DR rehearsal itself works; the owner just never receives the confirmation, so a future *failed* rehearsal could also go unseen. Verify `PUSHOVER_APP_TOKEN`/`PUSHOVER_USER_KEY` in homeserver `.env.secrets` and send a manual test POST to api.pushover.net; also confirm Pushover delivery history for `backup.sh` success alerts (couldn't be checked read-only). Surfaced during OA-5 verification | 2026-07-26 | Troy | Entry 188 / OA-5 spinoff | OPEN |
| OA-17 | **Private-repo regression (from OA-2 flip):** the homeserver git repo `/mnt/user/appdata/open-brain` has an unauthenticated HTTPS `origin` → `git fetch`/`git checkout origin/main -- <file>` now fails (`could not read Username for 'https://github.com'`). **This BLOCKS the documented compose/migration deploy pattern** (git-checkout a single file from origin/main — Entry 179). Fix: either `git remote set-url origin git@github.com:davistroy/open-brain.git` + add a repo **deploy key** to the homeserver's SSH (github.com), OR configure an HTTPS credential helper with a fine-grained PAT. Verify with `git -C /mnt/user/appdata/open-brain fetch origin`. **Do before the next compose/migration deploy.** (Minor sibling: the VM's `gh` fine-grained PAT lost `gh pr checks` on the now-private repo — grant it `checks:read`+`statuses:read`; workaround = Actions API.) | 2026-07-21 | Troy | Entry 192 / OA-2 regression | OBSOLETE 2026-07-14 (Entry 195): resolved by reverting to public — homeserver anonymous `git fetch origin` VERIFIED working; `gh pr checks` VERIFIED working. No deploy key / PAT scope needed while public. |

## Completed Actions

| ID | Action | Completed | Source |
|----|--------|-----------|--------|
| — | (none yet — items move here with `DONE YYYY-MM-DD` when finished) | — | — |
