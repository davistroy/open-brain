{
  "name": "open-brain-actual-ingest",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Actual Budget daily ingestion sidecar (issue #311). Standalone Node project outside the pnpm workspace — its own package-lock.json, mirroring cloudflare/email-worker.",
  "scripts": {
    "start": "node actual-daily.mjs",
    "test": "vitest run"
  },
  "dependencies": {
    "@actual-app/api": "^26.7.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "vitest": "^3.2.6"
  }
}
