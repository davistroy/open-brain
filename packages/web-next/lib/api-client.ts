// Legacy barrel — preserved for backward compatibility with existing
// consumers. The actual implementation lives in `lib/api/<domain>.ts`.
// Future imports should use `@/lib/api/<domain>` directly.

export * from './api'
